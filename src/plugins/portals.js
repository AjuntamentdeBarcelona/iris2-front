import Vue from 'vue'
import PortalVue from 'portal-vue'

Vue.use(PortalVue, {
  portalName: 'portal-source', // default: 'portal', changed in prevision of the new Google's Portals api
})

// Now you're thinking with portals.
